 <!-- Fixed navbar -->
    <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" <%= link_to 'Home', tweets_path %></a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <li class="active"><%= link_to 'Users', users_path %></li>
            <li class="active"><%= link_to 'Groups', groups_path %></li>
            <li class="active"><%= link_to "Bootstrap", '/bootstrap', :method => :get%></li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.email %> <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %> </li>                
                <li role="separator" class="divider"></li>
                <li class="dropdown-header">Session Control</li>
                <li><%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %></li>
               </ul>
            </li>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </nav>



  <p>
<h1 class="index-header">                                                    
  <div class="col-md-12 text-center">
  <label class="label label-default">Users</label>
</div>
</h1>

      </div>
      <div class="row">
        <div class="col-md-12">
            <table class="table">
            <thead>
              <tr>
      <th>Name</th>
      <th>Bio</th>
      <th>Age</th>
      <th>Email</th>
      <th>Ilość tweetow</th>
      <th>Id grup</th>

             </tr>
            </thead>
 <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.bio %></td>
        <td><%= user.age %></td>
        <td><%= user.email %></td>
         <td><% if user.tweets.any?%>
           <%= user.tweets.count %>
                             
         <% end %></td>
        <td class="label label-info"><%= user.groups.pluck(:name).join(' ') %></td>
        <td><%= link_to 'Show', user , class: 'btn btn-xs btn-warning'%></td>
        <td><%= link_to 'Edit', edit_user_path(user) , class: 'btn btn-xs btn-info'%></td>
        <td><%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-xs btn-danger' %></td>
      </tr>
    <% end %>
  </tbody>
          </table>
        </div>




</div>
